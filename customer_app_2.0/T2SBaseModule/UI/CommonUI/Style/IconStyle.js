import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    iconContainer: {
        width: 30,
        height: 30,
        justifyContent: 'center'
    },
    // for Icon Button
    iconButtonStyle: {
        marginHorizontal: 4
    }
});
